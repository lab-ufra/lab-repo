//elementos da página
const data_input = document.getElementById('score_in');

const invalid_input = "Nota inválida. Digite um valor entre 0 e 10."
const success = "A nota foi salva com sucesso."

export { data_input, invalid_input, success }